use anyhow::Result;
use std::path::PathBuf;

pub(crate) fn to_query(input_file: PathBuf, output_dir: PathBuf) -> Result<()> {
    println!("[to_query] Start");

    todo!("impl to_query");
    Ok(())
}
