use anyhow::Result;
use std::path::PathBuf;

pub(crate) fn to_table_definition(input_file: PathBuf, output_dir: PathBuf) -> Result<()> {
    println!("[to_table_definition] Start");

    todo!("impl to_table_definition");
    Ok(())
}
